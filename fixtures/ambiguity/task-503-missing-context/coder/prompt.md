You are the coder agent. The request references a non-existent class:
- "Update the UserService class"
- But UserService does not exist in the codebase

Requirements:
- Cannot produce a meaningful patch for non-existent code
- Document the missing context

Return a patch acknowledging the missing context.

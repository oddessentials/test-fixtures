Using the architect's spec for missing context, produce a task list
that acknowledges the non-existent reference.

The request references: UserService class
But: UserService does not exist in the codebase

Requirements:
- Emit "unknown" task type for the missing context
- Document what information is needed

You are the coder agent. Apply the architect and planner guidance to fix
the SQL injection vulnerability.

Requirements:
- Fix the SQL injection vulnerability in src/db/query.ts
- Use parameterized queries instead of string concatenation
- This is a security-critical change

Return a unified diff patch that fixes the vulnerability.

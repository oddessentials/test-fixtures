You are the coder agent. Apply the architect and planner guidance to implement
checksum validation.

Requirements:
- Create src/integrity/checksumComputer.ts with hash computation
- Create src/integrity/validator.ts with validation logic

Return a unified diff patch that implements checksum validation.

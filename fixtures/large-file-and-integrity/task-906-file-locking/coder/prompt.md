You are the coder agent. Apply the architect and planner guidance to implement
file locking.

Requirements:
- Create src/files/lockTypes.ts with lock types
- Create src/files/lockManager.ts with lock management logic

Return a unified diff patch that implements file locking.

Using the architect's spec for atomic file writes, produce a task list
for implementing atomic write operations.

Requirements:
- Implement atomic write logic
- Use temp file + rename pattern
- Handle errors and cleanup

You are the coder agent. Apply the architect and planner guidance to implement
atomic file writes.

Requirements:
- Create src/files/atomicWriter.ts with atomic write logic
- Use temp file + rename pattern for atomicity

Return a unified diff patch that implements atomic file writes.

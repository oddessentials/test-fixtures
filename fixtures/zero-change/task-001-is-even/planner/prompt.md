The `isEven` utility is incorrectly implemented. Using the architect's spec for this tiny
utility, produce **one low-complexity task** that fixes the `isEven` function in
`src/utils/isEven.ts` and nothing else.

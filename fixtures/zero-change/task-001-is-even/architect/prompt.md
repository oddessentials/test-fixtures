You are designing a tiny spec for a utility function `isEven(n: number): boolean`.

Requirements:

- `isEven(2)`, `isEven(0)`, `isEven(-4)` must be `true`.
- `isEven(1)`, `isEven(-3)` must be `false`.

Non-goals:

- Do **not** change any other utilities.
- Do **not** introduce new features or APIs.
- Only clarify the behavior of `isEven`.

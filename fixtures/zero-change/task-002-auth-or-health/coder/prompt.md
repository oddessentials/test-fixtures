Please implement the following tasks:

1. **add-health-endpoint**: Add GET /health endpoint to src/server.ts that returns { status: 'ok' } with 200 status code and no authentication.

2. **add-health-test**: Add test for /health endpoint in test/routes/health.test.ts to verify it returns correct status.

Current repository state: Basic Express server with /api/users endpoint, missing health endpoint.

Using the architect's spec for tracking dependencies, produce a task list
that documents the dependency graph.

Requirements:
- Include tasks for documenting dependencies
- Track which files depend on which other files

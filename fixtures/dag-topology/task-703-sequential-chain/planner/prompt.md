Using the architect's spec for a sequential chain, produce a task list
with strict sequential dependencies.

Chain: Task 1 → Task 2 → Task 3 → Task 4

Requirements:
- Each task depends on the previous one
- No parallel execution

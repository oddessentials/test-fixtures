You are the coder agent. Apply the architect and planner guidance to implement
cycle detection for task dependencies.

Requirements:
- Create src/graph/types.ts with graph types
- Create src/graph/cycleDetector.ts with cycle detection algorithm

Return a unified diff patch that implements cycle detection.

Using the architect's spec for parallel independent tasks, produce a task list
with no dependencies between tasks.

Requirements:
- Emit multiple tasks with empty dependsOn arrays
- All tasks can run in parallel

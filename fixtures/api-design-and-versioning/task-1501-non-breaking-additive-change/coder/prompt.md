You are the coder agent.
Implement the plan: Update `src/items.ts` to support optional `sort` parameter.

Rules:
- If `query.sort === 'name'`, return items sorted by name ascending.
- Otherwise return items in original order.
- Do not change the `Item` interface.
- Keep the change minimal.

You are the reviewer agent.
Analyze the patch for `src/items.ts`.
It adds a `sort` feature.
Verify:
- It is backwards compatible (no sort if param missing).
- It sorts correctly by name.
- No unrelated changes.

If good, approve.

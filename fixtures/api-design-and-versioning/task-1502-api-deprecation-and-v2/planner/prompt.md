You are the planner agent.
Plan the implementation of `GET /v2/users` in `src/users.ts`.
- `v2` returns objects with `displayName` instead of `username`.
- `v1` interface should mark `username` as `@deprecated`.
- Ensure v1 keeps working.

Generate a plan.

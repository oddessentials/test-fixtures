You are the coder agent.
Implement `UserV2` and `getUsersV2` in `src/users.ts`.
- `UserV2` has `id` and `displayName`.
- `getUsersV2` should return the same users but mapped to `displayName`.
- Add `/** @deprecated Use UserV2 and displayName instead */` to `UserV1.username`.

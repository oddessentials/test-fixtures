You are the reviewer agent.
Review the patch for `src/users.ts`.
- `v1` should be deprecated but working.
- `v2` should be functional and return `displayName`.
- No breaking changes.

Approve if correct.

You are the coder agent.
Implement `src/utils/AppError.ts`.
It should extend properties: statusCode (number), status (string), isOperational (boolean).
Constructor should take message and statusCode.

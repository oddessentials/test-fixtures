You are the coder agent.
Implement `src/utils/logger.ts`.
It should export a `logger` object with `info`, `error`, `warn` methods.
Each method should print a JSON string with timestamp, level, and message.

diff --git a/src/lib/config-loader.ts b/src/lib/config-loader.ts
index 1111111..2222222 100644
--- a/src/lib/config-loader.ts
+++ b/src/lib/config-loader.ts
@@ -10,7 +10,10 @@ export const loadConfig = (env: string) => {
     };
     // If we use Object.assign, it replaces the 'payment' object entirely, losing 'key' if we didn't specify it in prod?
     // Or if we want to merge.
-    return Object.assign({}, base, prod);
+    return {
+      ...base,
+      ...prod,
+      payment: { ...base.payment, ...prod.payment }
+    };
   }
   return base;
 };

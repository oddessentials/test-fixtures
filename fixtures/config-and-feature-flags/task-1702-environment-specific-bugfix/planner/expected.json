{
    "tasks": [
        {
            "id": "1",
            "title": "Fix Config Merge Logic",
            "description": "Update loadConfig to merge nested payment properties instead of replacing the object.",
            "filePaths": [
                "src/lib/config-loader.ts"
            ],
            "type": "bug",
            "dependency": []
        }
    ]
}
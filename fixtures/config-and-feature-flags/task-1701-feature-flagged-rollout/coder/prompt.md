You are the coder agent.
Implement the feature flag.
1. Modify `src/config.ts` to export `ENABLE_DARK_MODE` (boolean, default false).
2. Create `src/theme.ts` that returns "dark" if flag is true, else "light".

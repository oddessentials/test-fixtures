You are the planner agent.
Plan the rollout of `ENABLE_DARK_MODE` flag.
Steps:
1. Update `src/config.ts` to read `ENABLE_DARK_MODE` from env.
2. Update application logic (e.g. `src/theme.ts`) to use the flag.

Generate tasks.

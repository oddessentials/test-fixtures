Using the architect's spec for moving a file and updating imports, produce a task list
that covers the file move and all import updates.

Move: src/helpers/utils.ts → src/utils/helpers.ts

Requirements:
- Include task for moving the file
- Include tasks for updating imports in all consuming files

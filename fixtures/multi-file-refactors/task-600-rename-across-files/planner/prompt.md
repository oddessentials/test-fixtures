Using the architect's spec for renaming across files, produce a task list
that covers all files needing updates.

Rename: getUserData → fetchUserProfile

Requirements:
- Include tasks for each file that needs updating
- Ensure all imports and references are covered

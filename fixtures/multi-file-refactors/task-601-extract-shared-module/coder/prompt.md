You are the coder agent. Apply the architect and planner guidance to extract
shared validation logic into a new module.

Requirements:
- Create src/utils/validation.ts with common validation functions
- Update src/forms/login.ts to import from the shared module
- Update src/forms/register.ts to import from the shared module

Return a unified diff patch that extracts the shared module.

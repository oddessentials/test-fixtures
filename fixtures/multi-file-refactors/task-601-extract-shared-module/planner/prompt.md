Using the architect's spec for extracting shared code, produce a task list
that covers creating the shared module and updating all files.

New shared module: src/utils/validation.ts
Files to update: src/forms/login.ts, src/forms/register.ts

Requirements:
- Include task for creating the shared module
- Include tasks for updating each file to use the shared module
